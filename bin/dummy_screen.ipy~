import subprocess
import shlex

def dummy_screen(HTSOHM_dir, gen, start, length):

    %cd {HTSOHM_dir}/bin
    
    print('Screening dummies...')
    print('See terminal for status.') 
    subprocess.call( shlex.split( './screen ' + gen))
    print('\nJobs submitted!')
    %cd {HTSOHM_dir}
