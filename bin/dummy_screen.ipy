import subprocess
import shlex


def dummy_screen(HTSOHM_dir, gen):

    %cd {HTSOHM_dir} / bin

    print('Screening dummies...')
    print('See terminal for status.')
    subprocess.call(shlex.split('./dummy_screen ' + gen))
    print('\nJobs submitted!')
    %cd {HTSOHM_dir}
