#!/bin/bash

for file in $(ls *); do
  vi +':%s///g' +':wq' ${file}
#  echo "$file"
#  echo ${file: -3}

  if [ ${file: -3} = ".py" -o ${file: -4} = ".ipy" ] ; then
#  then
#    echo "python"
    autopep8 --in-place --aggressive --aggressive "$file" 
  fi

done;
