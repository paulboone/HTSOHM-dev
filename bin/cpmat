#!/bin/bash
#while [ -e "$(find ./ -name *.cif -printf '%h' -quit)" ]

LIBRARY_SIZE=$1
START_MAT=$2

export CIF_DIR=${HOME}/RASPA/share/raspa/structures/cif/

for (( i = START_MAT; i <= START_MAT+LIBRARY_SIZE; i++ ))
do    
    cd *MAT-$i
    cp *.cif $CIF_DIR
    cd ..
done
